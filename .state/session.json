{
  "schema": 2,
  "project": "lab-management-system-vue",
  "current_task": "Phase 2d-2 Select 收官（全仓无 raw <select>）+ ReceiptsList label 配对重构 → tag v0.3.50-20260905",
  "last_session_at": "2026-09-05",
  "last_session_summary": "Phase 2d-2 完成，src/ 下不再有任何 raw <select>（grep 命中全是注释）。上半场（前一会话）迁 InspectionCapabilityList 6 / CalculationMethodList 4 / TechnicalRequirementList 3 / DefaultParamCard 3；本会话收尾 6 文件 15 处。4 张数据录入卡 10 处：RebarMechNumericCard 3（技术要求/整体单项评定/逐行断裂位置）、RebarWeldingTensileCard 3（技术要求/整体单项评定/逐行断裂特征）、RebarWeldingBendCard 2（整体单项评定/逐行弯曲结果）、StrengthCardBase 2（技术要求/单项评定，两个 handler 签名从 (e: Event) 改 (v: string|number)）。DefaultParamCard 补正：删掉 Phase 2d-1 留下的 3 个悬空 for（for=standardCode 等指向不存在的 id —— 卡片按参数重复挂载 id 必撞，没法配对），改 SelectTrigger aria-label。ReceiptsList 5 select + 14 label：流程状态筛选 FlowFilter '' 改 '__all__' sentinel（load() 只认 receiving/submitted 所以天然不下发 flowStatus）；新建/编辑弹窗各 2 处（检测类别/样品来源），选项字面量提成 TEST_CATEGORIES / SAMPLE_SOURCES 去掉 6 处重复；14 个 raw <label> 包裹式关联改 <Label for> + 控件 id 显式配对（Phase 1.4 特意跳过留到这里：reka-ui 触发器是 <button role=combobox>，包在 <label> 里点击语义打架 —— label 激活 button = 开下拉），id 前缀 receipt-create-* / receipt-edit-* 分开防以后同时渲染。测试 295 → 298：cardsAll 新增 11 条锚、receiptsList 新增 4 条，其中 4 条是行为断言不是形态断言（锁 __none__ 哨兵翻译回路 + __all__ 不下发 flowStatus），做过变异验证 —— 把 `v === NONE ? '' : String(v)` 改成 `String(v)` 锚测立刻变红。新增 tests/selectInteraction.ts 共享 openSelect / pickSelectItem。过了 code-reviewer 两段审查，无阻断项；4 条「应该改」全部落实。gate exit 0，L0/L0.no_fallback/L0.5/L1/L2/L3/L4/L5 全 PASS，100 功能条目引用完整，软告警 0 条。",
  "done": [
    "RebarMechNumericCard.vue 3 处 <select> → <Select>（技术要求 / 整体单项评定 / 逐行断裂位置）",
    "RebarWeldingTensileCard.vue 3 处（技术要求 / 整体单项评定 / 逐行断裂特征）",
    "RebarWeldingBendCard.vue 2 处（整体单项评定 / 逐行弯曲结果）",
    "StrengthCardBase.vue 2 处（技术要求 / 单项评定；onReqChange + onManualVerdictChange 签名改 (v: string|number)）",
    "DefaultParamCard.vue 删 3 个悬空 for + 补 SelectTrigger aria-label + 字面 '__none__' 收敛成 NONE 常量",
    "ReceiptsList.vue 5 处 <select> → <Select>（列表筛选 FlowFilter '' → '__all__' sentinel；两弹窗检测类别 + 样品来源）",
    "ReceiptsList.vue 14 处 raw <label> → <Label for> + 控件 id（receipt-create-* / receipt-edit-* 双前缀）",
    "ReceiptsList.vue 选项字面量提成 TEST_CATEGORIES / SAMPLE_SOURCES（去两弹窗 6 处重复）",
    "tests/selectInteraction.ts 新增（openSelect / pickSelectItem 从 foundation 测试提取共享）",
    "cardsAll.dom.test.ts Phase 2d-2 锚 11 条（含 3 条哨兵翻译行为断言，走 <Select> 组件边界 $emit）",
    "receiptsList.dom.test.ts Phase 2d-2 锚 4 条（含 load() 参数契约断言：__all__ 不下发 flowStatus）",
    "变异验证：破坏 NONE 翻译 → 2 条锚测变红；还原 → 51/51 绿",
    "code-reviewer 两段审查通过，无阻断项，4 条「应该改」全部落实",
    "vitest 298/29 全绿（基线 283 + 本次 15）",
    "vue-tsc --noEmit 零错",
    "gate -p lab-management-system-vue exit 0，L0/L0.no_fallback/L0.5/L1/L2/L3/L4/L5 全 PASS",
    "CHANGELOG 0.3.50 条目（6 文件 15 处 + 14 label 配对 + 15 锚 + 5 条踩坑 + 验证）"
  ],
  "next": [
    "deploy：推 tag v0.3.50-20260905 → CI docker build + VPS pull + restart",
    "Phase 2e: Dialog + AlertDialog 原语 + 14 个 modal 分类迁移（确认型 6 → AlertDialog；表单型 4 + 列表关联型 2 + 预览型 3 → Dialog）。本 phase 风险最高：Teleport/DialogPortal stub、ESC、backdrop click、焦点循环都要肉眼验",
    "Phase 2f: NavigationMenu（SidebarNav 结构改造）+ Tailwind 主题收敛（text-green-600 / text-red-600 / text-gray-400 → semantic token）",
    "Phase 3: eslint-plugin-vue（走 /tree-change 加 version-lock）+ vue/no-restricted-html-elements 禁 raw 原语 + bundle 增量记账到 docs/conventions/bundle-budget.md",
    "Phase 3 待清理（本次发现）：RebarWeldingBendCard 整体单项评定 <Select> 是死分支（parseBendRecord 把 results 归一成 合格|不合格，overall 恒非空，v-else 永不渲染）—— 要产品确认这个兜底还要不要",
    "Phase 3 待清理：NONE / TRIGGER_CLS 在 4 张卡各定义一份，可提共享常量"
  ],
  "open_questions": [],
  "dont": [
    "不要 <input type=radio>（留 Phase 2g RadioGroup）",
    "不要在卡片里裸用 <Label>（父级 text-xs 继承会被基类 text-sm 覆盖，必须显式写 text-xs）",
    "不要把 wrapping 模式（<Label> 包 <Input>/<Select>）拆成 for/id 配对（保留 wrapping 模式）",
    "不要给 Label.vue 加 variant（它是手写原语不是 CVA）",
    "regression-anchor 测试 it() 标题禁止嵌入 Mxx.Fxx.Ixx 字面（fnReporter leak）",
    "不要把 Input.vue modelValue 再改回 string-only（会触发 reactive Record TS2322 回归）",
    "不要给 v-model 加 .lazy modifier（搜索框 @keydown.enter / @keyup.enter 仍依赖 input 事件即时同步）",
    "不要在 <Input>/TableCell/TableHead/TableRow 单值 class prop 上用 :class=\"[]\" 数组（class 是 string 不是 string[]，要拼接）",
    "不要断言 TableCell 基类被 twMerge 吃掉（inheritAttrs:false + v-bind=\"$attrs\" 让调用方 class 与 cn() 基类**拼接**不是二选一，p-2 仍在串里；要断言就断言 indexOf 顺序）",
    "不要 <Input @change=\"(e) => ...\"> 省略 Event 类型注解（L3 TS7006）",
    "不要在 Phase 2b 之后把 find('.text-green-600') 改了（class 字面量 → semantic token 走 Phase 2f 全家族一起收敛）",
    "不要指望 div-based Table 支持 rowspan/colspan（ParticleGradationCard 序号 cell 的 rowspan=3 是惰性属性，保留只为与已迁文件 colspan 处理一致）",
    "不要照抄模板列数写回归锚（SoilCompactionDegreeCard 默认 config 是 9 列不是 10 列，showMaxDensityColumn 未开）",
    "不要给 Markdown 加 `+` 列表项（MD004 ul-style 期望 dash，已统一为 `-` 缩进续行）",
    "不要把 <Checkbox> modelValue 放宽到 string|number|boolean（reka-ui CheckboxRoot update 泛型推导会触发 TS2345：$event 走 string|number|boolean，emit 期望 boolean|indeterminate；走 readBool/writeBool helper 在 === true 处收敛才是稳的）",
    "不要在原生 HTML checkbox 上断言（Phase 1.5 审计原则的合理例外：Checkbox 是 a11y 升级，迁 <Checkbox> 后 selector 必须 [role=checkbox] 不是 input[type=checkbox]）",
    "不要用 rows=\"4\" 写法（HTML attribute 永远 string，触发 Vue dev warn；用 :rows=\"4\" JS 表达式才对得上 number prop）",
    "不要给 SelectItem value=\"\"（reka-ui SelectItem 显式禁止：空串保留给 placeholder；「全部 / 不限」sentinel 用 '__all__' 之类非空字符串，load() 里翻译回空串）",
    "不要把 aria-label / data-fn / id 挂在 <Select> 上（reka-ui SelectRoot 是 Fragment，$attrs 不会 propagate 到 SelectTrigger；必须挂在 <SelectTrigger>）",
    "不要用 trigger.trigger('click') 触发 Select 打开（reka-ui SelectTrigger 监听 pointerdown 开门，不是 click；SelectItem 用 pointerup 选中，不是 click 也不是 pointerdown；jsdom 测试需 dispatch native PointerEvent 或 fallback 到 MouseEvent + 给 element 补 hasPointerCapture = () => false）",
    "不要给 <Label> 加指向不存在 id 的 for（悬空 for 对读屏比没有更糟；重复挂载的组件用 aria-label 不用 for/id —— Phase 2d-1 在 DefaultParamCard 犯过，2d-2 已清）",
    "不要指望 <SelectValue> 首帧就回显选中项文本（reka-ui 关闭态把 items teleport 进 DocumentFragment 注册 value→text，fragment 本身在 SelectContent onMounted 才建；测试断言回显必须先 await flushPromises，产品侧无影响）",
    "不要在裸 mount() 下用 openSelect / pickSelectItem（只在 Teleport 被 stub 即 mountWithProviders 时有效；裸 mount 走真实 portal 打不通选中回路，改从 <Select> 组件边界 $emit('update:modelValue', v) 验业务 handler）",
    "不要只写形态断言就当锚测（Phase 2d-2 审查教训：只断言 combobox 存在 / 无 raw select，锁不住 __none__ 哨兵翻译回路；哨兵/翻译这类逻辑必须写行为断言并做变异验证）"
  ],
  "active_branch": "master"
}
